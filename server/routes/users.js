const express = require('express');
const router = express.Router();

// Placeholder user routes - you can implement these later
router.get('/', (req, res) => {
  res.json({ message: 'User routes will be implemented soon' });
});

module.exports = router;